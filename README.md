Per runnare il server in locale php usare:
php -S localhost:8000
phpmyadmin:
http://localhost/phpmyadmin
user: root
password:root
host: localhost

per copiare 
git clone "sshdellarepodigit dal tastino verde"
i cambiamenti li aggiungo con:
git add .
li commento con:
git commit -m "commento"
li carico su github:
git push
per controllare che tutto e' aggiornato uso:
git status
